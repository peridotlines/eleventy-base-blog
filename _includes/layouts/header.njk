<header class="page-header">
    <h1><a class="home-link" href="/">Peridotlines</a></h1>
        <ul class="nav-list">
            {%- for entry in collections.all | eleventyNavigation %}
    	        <li class="nav-item"><a href="{{ entry.url }}"{% if entry.url == page.url %} aria-current="page"{% endif %}>{{ entry.title }}</a></div>
    	    {%- endfor %}
        </ul>
</header>